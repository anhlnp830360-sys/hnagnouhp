SELECT
    w.WarehouseID,
    w.WarehouseAddress,
    COUNT(DISTINCT i.ProductID) AS NumberOfProducts
FROM Warehouse w
JOIN Instock i
    ON w.WarehouseID = i.WarehouseID
GROUP BY
    w.WarehouseID,
    w.WarehouseAddress
HAVING COUNT(DISTINCT i.ProductID) > 3;
