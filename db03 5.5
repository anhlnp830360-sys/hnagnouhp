SELECT 
    w.WarehouseID, 
    w.WarehouseAddress, 
    SUM(i.Quantity) AS TotalQuantity
FROM 
    Warehouse w
LEFT JOIN 
    Instock i ON w.WarehouseID = i.WarehouseID
GROUP BY 
    w.WarehouseID, 
    w.WarehouseAddress;
