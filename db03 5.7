SELECT 
    w.WarehouseID, 
    w.WarehouseAddress, 
    COUNT(i.ProductID) AS NumberOfProducts
FROM 
    Warehouse w
LEFT JOIN 
    Instock i ON w.WarehouseID = i.WarehouseID
GROUP BY 
    w.WarehouseID, 
    w.WarehouseAddress;
